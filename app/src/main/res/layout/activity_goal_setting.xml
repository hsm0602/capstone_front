<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F6F7FB"
    tools:context=".GoalStateActivity">

    <!-- 🔹 top status bar -->
    <View
        android:id="@+id/statusBar"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="#DEE7FF"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


    <!-- 🔹 back icon (id를 tvBack으로 변경: Kotlin 코드와 맞추기) -->
    <ImageView
        android:id="@+id/tvBack"
        android:layout_width="18dp"
        android:layout_height="24dp"
        android:src="@mipmap/ic_launcher"
        android:layout_marginStart="24dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/statusBar"
        app:layout_constraintStart_toStartOf="parent"
        app:tint="#5A68FF" />

    <!-- 🔹 Title (목표 설정) -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="목표 설정"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="#5A68FF"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/statusBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 🔹 Subtitle -->
    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="체지방 감소 및 다이어트"
        android:textSize="15sp"
        android:textColor="#8F959D"
        android:layout_marginTop="36dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 🔹 목표 상태(= currentStatus였던 것) -->
    <TextView
        android:id="@+id/tvGoalStateTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="목표 상태"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="#3A3A3C"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvSubtitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


    <!-- 🔹 input boxes -->
    <LinearLayout
        android:id="@+id/inputArea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="32dp"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/tvGoalStateTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 키 (cm) - etHeight 로 아이디 변경 -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="56dp"
            app:cardCornerRadius="16dp"
            app:cardBackgroundColor="#EAF0FF"
            app:cardElevation="0dp"
            android:layout_marginBottom="16dp">

            <EditText
                android:id="@+id/etHeight"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:hint="키 (cm)"
                android:textColor="#738CFF"
                android:textSize="16sp"
                android:inputType="numberDecimal" />
        </androidx.cardview.widget.CardView>

        <!-- 체중 (kg) - etWeight -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="56dp"
            app:cardCornerRadius="16dp"
            app:cardBackgroundColor="#EAF0FF"
            app:cardElevation="0dp"
            android:layout_marginBottom="16dp">

            <EditText
                android:id="@+id/etWeight"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:hint="체중 (kg)"
                android:textColor="#738CFF"
                android:textSize="16sp"
                android:inputType="numberDecimal" />
        </androidx.cardview.widget.CardView>

        <!-- 체지방량 (%) - etBodyFat -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="56dp"
            app:cardCornerRadius="16dp"
            app:cardBackgroundColor="#EAF0FF"
            app:cardElevation="0dp">

            <EditText
                android:id="@+id/etBodyFat"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:hint="체지방량 (%)"
                android:textColor="#738CFF"
                android:textSize="16sp"
                android:inputType="numberDecimal"/>
        </androidx.cardview.widget.CardView>

    </LinearLayout>


    <!-- 🔹 next button - btnNext 로 아이디 변경 -->
    <androidx.cardview.widget.CardView
        android:id="@+id/btnNext"
        android:layout_width="110dp"
        android:layout_height="55dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="32dp"
        app:cardCornerRadius="28dp"
        app:cardBackgroundColor="#4A64FF"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="다음"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"/>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>
