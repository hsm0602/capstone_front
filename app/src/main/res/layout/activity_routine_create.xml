<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- 🔹 Back Icon -->
    <ImageView
        android:id="@+id/backBtn"
        android:layout_width="20dp"
        android:layout_height="24dp"
        android:layout_marginStart="24dp"
        android:layout_marginTop="20dp"
        android:src="@drawable/ic_back"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="#2260FF" />

    <!-- 🔹 Blue title label -->
    <TextView
        android:id="@+id/labelRoutine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="루틴 생성"
        android:textSize="24sp"
        android:textColor="#2260FF"
        android:textStyle="bold"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/backBtn"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 🔹 Big Title -->
    <TextView
        android:id="@+id/todayGoal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Today's goal"
        android:textSize="32sp"
        android:textColor="#303437"
        android:textStyle="bold"
        android:layout_marginTop="28dp"
        app:layout_constraintTop_toBottomOf="@id/labelRoutine"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 🔹 Sub label (Figma에 있는 설명 텍스트) -->
    <TextView
        android:id="@+id/subLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="목표에 맞춘 오늘의 루틴을 생성하세요."
        android:textColor="#828A8F"
        android:textSize="15sp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/todayGoal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 🔹 예시 박스 (White Card) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/exampleCard"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:layout_marginHorizontal="24dp"
        app:cardCornerRadius="22dp"
        app:cardElevation="5dp"
        app:cardUseCompatPadding="true"
        app:layout_constraintTop_toBottomOf="@id/subLabel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <TextView
                android:id="@+id/exampleTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="예시"
                android:textColor="#828A8F"
                android:textSize="15sp" />

            <TextView
                android:id="@+id/example1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:text="→ 발목을 삐었어 고려해서 루틴 짜줘"
                android:textColor="#303437"
                android:textSize="15sp" />

            <TextView
                android:id="@+id/example2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:text="→ 오늘은 어깨를 집중적으로 하고싶어"
                android:textColor="#303437"
                android:textSize="15sp" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- 🔹 파란 입력창 (아래쪽) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/inputGoalCard"
        android:layout_width="338dp"
        android:layout_height="54dp"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="28dp"
        app:cardBackgroundColor="#ECF1FF"
        app:cardCornerRadius="14dp"
        app:cardElevation="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/exampleCard">

        <EditText
            android:id="@+id/editGoal"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:hint="목표를 입력해 주세요"
            android:paddingHorizontal="20dp"
            android:textColor="#2260FF"
            android:textColorHint="#738CFF"
            android:textSize="16sp" />
    </androidx.cardview.widget.CardView>

    <!-- 🔹 Next Button (오른쪽 아래 고정) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/nextBtn"
        android:layout_width="90dp"
        android:layout_height="52dp"
        android:layout_marginBottom="32dp"
        android:layout_marginEnd="24dp"
        app:cardCornerRadius="26dp"
        app:cardBackgroundColor="#2260FF"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_gravity="center"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="다음"
            android:textSize="17sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold"/>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>
